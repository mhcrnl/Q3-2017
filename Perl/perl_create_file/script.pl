#! /usr/bin/perl

package EditReadme;
=pod

=cut
use strict;
use warnings;

sub new {
	my $class = shift;
	my $self = { };
	bless $self, $class;
	return $self;
}
=pod
	New methods of the class
=cut

sub 